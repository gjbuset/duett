_duett() 
{
    local cur prev opts
    COMPREPLY=()
    cur="${COMP_WORDS[COMP_CWORD]}"
    prev="${COMP_WORDS[COMP_CWORD-1]}"
    opts=" -meny -versjon -eksternt -oppdater -utforsk -lokalt -brukar -printer -rediger-user -avinstaller -nullstill -skjerm -hjelp -help"
    if [[ ${cur} == -* ]] ; then
        COMPREPLY=( $(compgen -W "${opts}" -- ${cur}) )
        return 0
    fi
}
complete -F _duett duett
